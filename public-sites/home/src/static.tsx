/**
 * The home page has no demo, so what the built index.html carries is the
 * whole app. Rendered at build time by shared/vite-seo.ts; the client then
 * mounts App over it.
 */
export { default } from './App'
